defaults:
  - base

strategy: FedProx
fl:
  num_rounds: 50
  fraction_fit: 0.8
  fraction_evaluate: 0.5
  min_fit_clients: 5
  min_evaluate_clients: 3
  min_available_clients: 5

fedprox:
  proximal_mu: 0.1
